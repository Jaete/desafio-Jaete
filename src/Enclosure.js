export class Enclosure {
    constructor (number, capacity, biome, startingAnimals){
        this.number = number;
        this.capacity = capacity;
        this.biome = biome;
        this.startingAnimals = startingAnimals;
    }
}